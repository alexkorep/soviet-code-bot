from flask import render_template
# from models.user_status import UserStatusModel

def handle_apply(bot, chat_dest, company_code):
    # user_status = UserStatusModel.get(chat_dest)
    # print('user_status', user_status.__dict__)
    # history_dict = {
    #     "start": user_status.current_month,
    #     "company_code": company_code,
    # }
    # user_status.job_history.append(history_dict)

    # text = render_template('apply_success.txt', user_status=user_status)
    # bot.send_message(chat_dest, text)

    # user_status.current_month += 1
    # user_status.save()
    pass
